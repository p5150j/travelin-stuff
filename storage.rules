rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Anyone can view images/videos (needed for public blog)
      allow read: if true;
      // Only authenticated users (Patrick) can upload
      allow write: if request.auth != null;
    }
  }
}
